import React from "react";
import ReactDOM from "react-dom/client";
import PulseDelivery from "./pulse-delivery";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PulseDelivery />
  </React.StrictMode>
);
